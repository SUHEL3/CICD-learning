name: Day 4 practice

on:
  workflow_dispatch:

jobs:
  runner-inspection:
    runs-on: ubuntu-latest

    steps:
      - name: show data
        run: ls -l

      - name: show user
        run: whoami

      - name: show current directory
        run: pwd

      - name: show os info
        run: cat /etc/os-release
